/**
 * @file build/link.ts
 * @description provides information about the sync process.
 **/

const link = ( ) => 
{
	console.log( '[yugen-utils] | symlinks are disabled in this environment.' );
	console.log( '[yugen-utils] | use vite.local.ts and FOUNDRY_OUT_DIR in .env for live sync.' );
};

link( );
