# yugen-utils

Standalone shared utility module for yugen. projects in Foundry VTT.

Provides a unified interface under `globalThis.yugen_utils` for flags, sockets, system-agnostic parsing, string manipulation, and aggressive stylesheet cache busting.
